# Allowed drift between:
# - canonical: provisioning/case-1d/provisioning/roles
# - mirror:    provisioning/roles
#
# Keep this file minimal: each exception must be intentional and reviewed.
allowed_drift:
  - path: ng-siem/README.md
    kinds: [missing_in_canonical]
    reason: "Extra mirror-only documentation."
  - path: ng-soc/handlers/main.yml
    kinds: [content]
    reason: "Mirror restart handler differs for older service naming."
  - path: playbook-library/handlers/main.yml
    kinds: [content]
    reason: "Mirror handler keeps compatibility restart strategy."
